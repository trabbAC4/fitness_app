//Setting authentification 
//Node and express

const express = require('express'); // Load the 'express' module from npm
const myModule = require('./myModule'); // Load a custom module using a relative path

